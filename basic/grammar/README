1. python中的数据类型分为：可变数据类型和不可变数据类型
    1>. 分类
    可变数据类型：list, dict
    不可变数据类型：int, float, bool, complex, str, tuple
    2>. 区别
    可变数据类型可以通过方法修改变量的内容，而不改变该变量的引用，不可变数据类型只能通过改变对变量的引用来修改一个变量的值
    可变数据类型在给变量执行赋值操作时会改变对变量的引用

2. 可以通过函数id(o)拿到变量在内存中的地址，该函数返回一个int，当变量的引用发生改变时，拿到的内存地址也会发生变化

3. python中字典的key只能是不可变数据类型，value可以是任意数据类型，因为在给字典中存入值时会调用函数hash(o)返
回一个hashcode，该hashcode用来确定值在字典中的存储位置，hash()方法要求穿入的参数时一个不可变的数据类型，因此
字典中key的值必须是不可变数据类型

4. 函数的形参，实参，返回值传递的都是变量在内存中的地址，传参时将变量与变量的引用绑定，所以有返回值的函数必须使用变量
来接收，绑定传出数值的内存地址
