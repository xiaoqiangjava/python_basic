1. 函数参数和返回值问题
    当对一个函数传入的参数做=赋值操作时，不管传入的参数时可变数据类型还是不可变数据类型，都不会影响
    到函数外部的变量值

    如果一个函数传入的参数时可变数据类型：list, dict，那么当在函数内部调用相应的方法（append, extend）修改参数的值时，
    函数外部的变量值也会修改，因为调用方法修改参数的值时修改了内存中的值而不是内存对应的引用关系

    对一个list执行+=赋值操作时，本质上时调用list的extend（）方法实现了list的扩展，所以+=赋值操作也会修改函数外部对应
    变量的值
    list += list 不是list = list + list操作